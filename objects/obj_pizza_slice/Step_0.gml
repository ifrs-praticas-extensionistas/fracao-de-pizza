show_debug_message(obj_pizza.slices)
show_debug_message(obj_pizza.slice_count)
show_debug_message(obj_pizza.slice_size)

x += aspeed;
aspeed += 5

if(x >= obj_pizza_plate.x){
	instance_destroy()
	obj_pizza.slices[slice_index].animated = false
}